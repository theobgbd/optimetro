#!/usr/bin/python
# Line class 
class Line:
    self.stations = list() # Stations IDs
    self.trains   = list() # Trains IDs
    self.color    = default_color
    
    def __init__(self, color):
        self.color = color

    def add_Station(new_station, target_station) :
        self.stations_list = append(new_station)
        
    def remove_Station(old_station) :
        self.station_list = remove(old_station)

    def reorder_Stations() :
        self.stations = shuffle()
        
    def add_Train() :
    
